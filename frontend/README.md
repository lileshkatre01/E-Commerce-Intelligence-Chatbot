# Frontend

This directory is reserved for Step 2 of the project.
You can implement the UI using:
- **Streamlit** (simple Python-based interactive dashboard)
- **React / Next.js** (modern web UI interacting with `http://127.0.0.1:8000/chat`)
- **HTML/CSS/JS** static frontend
